/*
    For this section of the assessment you will be putting together a cheat sheet for common git commands.  
    You'll provide the command as well as what it does. 
*/

//////////////////PROBLEM 1////////////////////
/*
    Create a variable called 'gitDefinition'.
    It should be a string containing your best definition of what Git is.
*/

let gitDefinition = "A version control system for code."

//////////////////PROBLEM 2////////////////////
/*
    Create a variable called 'gitHubDefinition'.  
    It should be a string containing your best definition of what GitHub is.
*/

let gitHubDefinition = "A website that acts as a cloud service to upload, save, share, and collaborate on code using git"

//////////////////PROBLEM 3////////////////////
/*
    Create a variable called 'gitInitDefinition'.  
    It should be a string containing your best definition of what 'git init' does.
*/

let gitInitDefinition = "git init is a command used in the terminal to initialize a new repository to begin using git's version control system."

//////////////////PROBLEM 4////////////////////
/*
    Create a variable called 'gitCloneDefinition'.  
    It should be a string containing your best definition of what 'git clone' does.
*/

let gitCloneDefinition = "git clone is a command used in the terminal to copy or 'clone' an existing repository onto your computer." 

//////////////////PROBLEM 5////////////////////
/*
    Create a variable called 'gitStatusDefinition'.  
    It should be a string containing your best definition of what 'git status' does.
*/

let gitStatusDefinition = "git status is a command used in the terminal that allows the developer to see the state of their files in their repository. It will show whether or not a file has been added and if its ready to be committed. "

//////////////////PROBLEM 6////////////////////
/*
    Create a variable called 'gitAddDefinition'.  
    It should be a string containing your best definition of what 'git add' does.

    Create another variable called 'gitAddCode'.  
    It should be a string containing the code to add all files.
*/

let gitAddDefinition = "git add is a command used in the terminal that developers can use to begin tracking changes to a chosen file. If git is initialized but a file is not added, those changes will go untracked by git. Git add helps you in allowing you to choose the files you will commit."
let gitAddCode = "git add ."

//////////////////PROBLEM 7////////////////////
/*
    Create a variable called 'gitCommitDefinition'.  
    It should be a string containing your best definition of what 'git commit' does.

    Create a variable called 'gitCommitCode'.  
    It should be a string containing the code to commit using the message "initial commit".
*/

let gitCommitDefinition = "git commit is a command used in the terminal that saves the project's history in your local repository. You must include a brief message about what was done to the file committed."
let gitCommitCode = " git commit -m 'initial commit.' "


//////////////////PROBLEM 8////////////////////
/*
    Create a variable called 'gitPushDefinition'.  
    It should be a string containing your best definition of what 'git push' does.
*/

let gitPushDefinition = "git push is a command used in the terminal that takes the 'snapshots' of the code's history in your local repository, and uploads it into a remote repository. GitHub is a common site developers use to upload these remote repositories."